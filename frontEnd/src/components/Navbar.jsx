const Navbar = () => {
  return (
    <nav className="bg-gray-800 text-white p-4 flex items-center justify-between fixed w-full z-50">
      <div className="text-lg font-semibold">Products Table</div>
    </nav>
  );
};

export default Navbar;
